apiVersion: v1
kind: Service
metadata:
  name: {{ template "firstkubernetes.fullname" . }}
  labels:
    app: {{ template "firstkubernetes.name" . }}
    chart: {{ template "firstkubernetes.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "firstkubernetes.name" . }}
    release: {{ .Release.Name }}
